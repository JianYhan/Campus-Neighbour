# Campus Neighbour 开发协作与任务计划

> **2026-09-19 实施更新：** 首版工程、测试及CI定义已建立。实际运行命令见[README](README.md)，测试证据及仍需完善的内容见[实施记录](docs/implementation/README.md)。后续功能继续采用TDD与PR审查；人员分工仍待填写。

版本：v0.1 完整初稿｜日期：2026年9月18日｜负责人、审核人：待填写。

状态：供团队评审，尚未实现或验证。具体业务默认值为建议基线；TDD开发方式已由项目发起人明确采用。

## 1 当前阶段和协作原则

当前仅提交文档；工程、依赖、自动检查尚未搭建。8份初稿先整体评审，再开展技术验证与开发。负责人、审核人和具体人员分工全部待填写。任务进度未来用GitHub Issues维护，文档记录依赖和规则。

**项目采用TDD（测试驱动开发）。** 这是已确定的开发方式：先用测试说明期望行为，再写实现。其余尚未批准的业务选择见[需求待定项](docs/requirements.md#9-待定事项与变更管理)。

## 2 每个功能的TDD工作循环

1. **明确一个小行为**：关联FR需求和验收场景，列清正常、边界及权限条件。
2. **Red：先写测试并运行，确认失败。** 检查是缺少目标行为导致失败；拼写、环境或无法连接数据库造成的失败不算有效Red。
3. **Green：写最少的必要实现，使测试通过。** 不提前加入未被需求要求的功能。
4. **Refactor：在测试保护下整理代码。** 去重复、改善命名和结构，再运行受影响测试，保持通过。
5. 完成一个小行为后再进入下一个；提交前执行相关集成测试与规定检查，同步接口及文档。

缺陷修复先加一个能复现缺陷的失败回归测试，再修复。若问题只能在真实浏览器或数据库中出现，就在相应层测试，不能用完全Mock的测试掩盖问题。

例如“商品只能预约给一人”：先写两个独立事务竞争同一商品的集成测试，期望一成功一冲突且只留一笔占用；观察当前失败，再实现事务和约束；最后在测试保护下整理服务代码。不要先写完预约功能再补一个永远通过的测试。

## 3 TDD的范围与证据

业务规则、权限、交易状态、消息校验、接口和页面交互遵循先测试后实现。测试验证用户可观察结果，不逐行复制实现；不以覆盖率百分比代替正确性。纯文字、静态文档和视觉探索使用审查／人工视觉验证；基础环境配置先定义可复现检查，搭建后执行，不为其机械添加无意义单元测试。

PR说明包含：需求编号、测试先行的行为、初始失败原因、最终检查结果和文档变化。可以记录本地Red/Green摘要或提交记录，不要求将失败测试合入main。CI只能证明最终测试状态，不能单凭绿灯证明过程符合TDD。

技术验证小样可用于探路；若代码保留到产品实现，必须补足行为测试并按小步循环整理。严禁将未执行测试写成“通过”。

## 4 分支、提交与评审

- main保持可评审、后续可构建；功能分支建议`feat/<issue>-<topic>`，修复`fix/...`，文档`docs/...`。
- 一个PR解决一个清楚的行为或紧密相关的小任务，避免整个模块数周后一次提交。
- 提交说明写清变化；接口／数据库变化与相关测试、文档放同一PR。迁移文件已执行后只追加。
- 合并前至少一位其他成员审查。分支保护和必需检查是后续配置任务，当前未启用承诺。
- 不提交密码、真实聊天、会话Cookie、个人开发配置、依赖目录和构建产物。演示数据使用虚构内容。

建议PR描述结构：问题与期望行为 → 变化 → Red/Green/重构说明 → 验证结果 → 关联文档与待处理事项。

## 5 代码与协作约定

前端React/TS严格类型，文件按auth/marketplace/chat/trades/profile等业务组织；后端按同类业务边界分包。具体lint和格式化配置在初始化时提交，不在此虚构已存在命令。

前端金额使用整数分；后端不接受客户端身份或任意状态。稳定错误码用大写下划线，枚举与[接口](docs/api/README.md)一致。英文代码标识符、中文团队说明；公共UI文案进入中英翻译资源，禁止把业务文本散落在组件中。

接口变化先在相关Issue说明影响，前后端共同确认后改契约；联调发现缺口时先增加失败测试。冲突由涉及成员在同一分支基线解决，不覆盖他人工作。负责人或审查人空缺不意味着由某成员默认承担。

## 6 任务包与依赖

任务包用于后续分配，不预先绑定9个人各写一个独立模块。每项负责人／审核人均待填写。

| 编号 | 工作包 | 依赖 | 完成标准 |
| --- | --- | --- | --- |
| W00 | 8份文档评审、D项决策、核心原型确认 | 当前初稿 | 评审意见形成决策记录并同步文档 |
| W01 | 技术组合验证、测试环境与CI | W00关键技术决定 | 构建、数据库、会话、消息最小验证有证据 |
| W02 | 账号与资料 | W01、D01/D02 | 权限测试先行，登录退出与公开字段隔离通过 |
| W03 | 商品／上传／列表／筛选 | W02、D03 | 发布到详情可运行，上传边界和归属测试通过 |
| W04 | 私聊与快捷消息 | W02/W03、D04/D10 | 三按钮、自由消息、越权与重试恢复测试通过 |
| W05 | 预约／确认／取消／评价 | W03/W04、D05/D06 | 并发和端到端买卖流程通过 |
| W06 | 校园简介／字典／季节专区 | W03、D08 | 数据与时间边界用例通过 |
| W07 | 一对一换物 | W05、D09 | 双商品占用及双向确认测试通过 |
| W08 | 管理／通知／支付展示 | W02/W05、D07 | 权限隔离、事件恢复、支付无副作用通过 |
| W09 | 联调、体验、部署、课程交付 | 上述相关功能 | 完整验收证据、恢复演练、用户说明与交付包 |

主题和双语贯穿各工作包，不能推迟到最后统一补。测试随每个任务开始，不设“所有开发结束后再补测试”的阶段。W09是系统整体验证和整理证据。

## 7 完成定义与开工门槛

开工前：需求／页面／接口／数据／测试一致；相关D项有明确选择；测试环境可运行。

任务完成：需求行为实现；Red/Green过程可说明；相关自动测试通过；无未处理的高影响缺陷；必要的手动主题／双语／移动端检查完成；接口与运行资料更新；他人审查完成。仅页面截图或自己电脑运行一次不算完成。

CI计划：前端类型与静态检查、行为测试和构建；后端测试与构建、PostgreSQL集成测试；核心浏览器流程；文档链接和契约检查。具体执行命令在工程建立后写入运行说明。

## 8 时间安排

课程截止均为2026年23:59 CST：申报书9月25日；Update 1为10月12日、2为11月2日、3为11月23日、4为12月7日（至少3次）；技术报告50%、软件30%、展示20%均12月14日提交。

开发建议：10月12日前基础流程与测试环境；11月2日前普通买卖；11月23日前校园特色与换物；12月7日前候选交付及系统验收；12月14日前完成提交。具体排期与负责人待团队评审，不替代课程要求。
